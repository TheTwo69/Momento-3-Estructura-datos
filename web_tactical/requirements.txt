# ─────────────────────────────────────────────────────────────────────────────
# LogiDrone-UCC — dependencias web_tactical
# Versiones fijadas para reproducibilidad entre entornos
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# WebSockets (incluido en uvicorn[standard], pero explícito por claridad)
websockets==12.0

# Templates HTML (Jinja2)
jinja2==3.1.4

# Archivos estáticos y formularios
python-multipart==0.0.9

# GIS (opcional — solo si usas geopandas/shapely en el código)
# Comenta estas dos líneas si no usas zonas GeoJSON reales
geopandas==0.14.4
shapely==2.0.4

# Utilidades
httpx==0.27.0   # cliente HTTP async (útil para tests)
